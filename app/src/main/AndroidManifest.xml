<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hrom.andrew.travelshops">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/main_icon_18"
        android:label="@string/app_name"
        android:theme="@style/MyTheme">
        <activity
            android:name=".ShopPages.MountainPage"
            android:label="@string/title_activity_travel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShopPages.BikePage" />
        <activity android:name=".ShopPages.SnowboardPage" />
        <activity android:name=".ShopPages.SkisPage" />

    </application>

</manifest>
